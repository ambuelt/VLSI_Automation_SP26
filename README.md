# VLSI_Automation_SP26 - ASU Spec2Tapeout ICLAD 2025 Problem

## Problem Set

Your objective is to design tapeout-ready ASICs based on the given problem specifications in the .yaml specification file.

### Setup Instructions:
Install dependencies for Python packages used:

```
pip3 install -r requirements.txt
```

Ensure that you have Codex CLI installed on your WBS Ubuntu environment as described at the following link: https://developers.openai.com/codex/cli

The following commands should make sure that you can run Codex CLI before running the program:

```
npm i -g @openai/codex
codex
```

You can run the docker image as follows to create an environment that has OpenROAD-flow-scripts and iVerilog already installed but the Codex CLI agent should do the following process for you:

```
docker run -it --rm \
  -v ~/iclad_hackathon:/workspace/iclad_hackathon \
  iclad_hackathon:latest bash
```

Inside the docker container, you will find this repository already cloned and available to you in 

```
/workspace/iclad_hackathon/ICLAD-Hackathon-2025/problem-categories
```

## Run the System:

Before runninng the code place the desired *.yaml file in the __[location]___

To run the **Finalized** version of the code run the following command in the WSB Ubuntu terminal:

```
python3 run_codex.py
```


## Our Directory Structure

```plaintext
Mini_Project_2/
├── README.md
├── run_codex.py
│   └── Acts as main agent script and will be used to run Codex CLI agent and run_sim.py
|
├── run_sim.py
│   └── Evaluates the Spec2RTL generated RTL and testbench for functional correctness
|
├── AGENTS.md
│   └── Acts as the Spec2RTL agent architecture and creates the agent feedback loop
├── PHYSICAL_AGENTS.md
│   └── Acts as the RTL2Tapeout agent architecture and creates the agent feedback loop
|
|
|
├── rtl_code/
│   ├── iteration_#.logs of automated run_codex.py script
|   ├── successful_run.txt states during which iteration the agent was successful (existance used as an indicator to start the OpenROAD process)
|
├── testbench_code/
│   ├── Contains all generated testbenches for visible problems
│   ├── Contains all provided testbenches for visible problems from ICLAD Github
│
|
├── logs/
│   ├── iteration_#.logs of automated run_codex.py script
|   ├── successful_run.txt states during which iteration the agent was successful (existance used as an indicator to start the OpenROAD process)
|
|
├── eda_agent/
│   └── rtl/
|       └── design.v - This is the RTL file generated during the manual Codex process
│   └── tb/
|       └── iclad_seq_detector_tb.v - This is the testbench file provided in the ICLAD Github
|       └── tb.v - This is the testbench file generated during the manual Codex process
│   ├── *.yaml file for testing. Visible input specifications for problem.
│           These are the primary inputs your LLM agent will consume to generate RTL and physical design outputs.
│           Each YAML file typically includes clock, module interface, and expected behavior.
│   ├── main.py, test.py, and tools.py are from old implmenetation but do not want to deleted yet in case it will be useful
│

```


## Required System Inputs

- *.yaml file for RTL and testbench specifications
- AGENTS.md file
- iteration_#.log files **only applicable after the first agent loop**


## Required System Outputs

- Synthesizable RTL in Verilog
- Synthesizable Testbench
- Constraint files in SDC format *(Set for Phase 3)*
- Tapeout-ready OpenROAD DB (ODB), i.e., 6_final.odb generated by ORFS *(Set for Phase 3)*


## Expected Results

Logs from the manual runthrough using Codex CLI according to the AGENTS.md instructions are included in the rtl_code folder



### Example of RTL Agent & OpenROAD Agent Outputs

- Place p1.yaml in __[location]___
- Run python3 run_codex.py in the Ubuntu terminal

Gives iVerilog log output at iteration 4 in logs folder: 

```
'SUCCESSFUL RTL DESIGN CREATED AT ITERATION 4'
```

Gives OpenROAD log output at iteration 2 in logs folder: 

```
'SUCCESSFUL RTL DESIGN CREATED AT ITERATION 2'
```


## Workflow Description:

The overall agentic workflow flows through the entire Spec2Tapeout design flow. Below gives high-level steps similar to the Figure flow diagrams provided in the final report and powerpoint.

Spec -> RTL/Testbench -> iVerilog Synthesis -> OpenROAD (Physical Design) -> Tapeout Ready .odb and .sdc

```
AGENTS.md
1. Agent reads *.yaml file for RTL and testbench specification
2. Codex CLI generates RTL and corresponding testbench
3. Run iVerilog simulation in Docker Environment
4. Capture log of current run
5. If iVerilog simulation fails
    - Debug and regenerate RTL
    - If iVerilog loops 10 times, exit loop and end agent
6. If successful, write successful_run.txt and move onto OpenROAD
```

```
PHYSICAL_AGENTS.md
1. Agent reads RTL file for specification
2. Generates SDC constraints from the RTL
3. Generates a config.mk for ORFS
4. Run OpenROAD simulation for synthesis and violatios in Docker Environment
5. Capture logs of current run
6. If OpenROAD simulation fails
    - Debug and regenerate RTL
    - If iVerilog loops 5 times, exit loop and start Spec2RTL agent
7. If successful, return .odb, .sdc, and RTL.v
```

More specifics about the general agent workflow are described in the AGENTS.md and PHYSICAL_AGENTS.md files.

## Evaluation setup - THE FOLLOWING WILL BE EDITED COME PHASE 3 SO WE THOUGHT TO INCLUDE IT NOW EVEN THOUGH ITS NOT HOW IT CURRENTLY RUNS!

For each problem you must run two evaluations scripts

### 1. Functional evaluation of Verilog

The run_sim.py script verifies that the Verilog passes functionality and the generated testbench script. This will check the correctness of your generated RTL using the provided testbenches and iVerilog and is run automatically with the agent generated testbench via the main run_codex.py automation. 

*Make sure to change the module name of the example problem*

To test the generated RTl against the provided testbench problem script an example command is provided below for p1.yaml:

```
python3 evaluate_verilog.py --verilog ../rtl_code/{module_name}.v --problem 1 --tb ../example_problem/intermediate/iclad_seq_detector_tb.v
```

### 2. Physical evaluation of layout metrics

This will check the metrics of your generated layouts using the specification jsons provided. An example command is provided below

*This part will be updated in the final version of the code - Phase 3*

## Running Hidden Testcases - THE FOLLOWING WILL BE EDITED COME PHASE 3 SO WE THOUGHT TO INCLUDE IT NOW EVEN THOUGH ITS NOT HOW IT CURRENTLY RUNS!

- Place hidden test case *.yaml in __[location]___
- Run python3 run_codex.py in the Ubuntu terminal
